# memory.md

AI agent entry point for the LMD project. Trigger keywords + file pointers only.

## Key Principles

1. **Standard library first**: Use Go standard library whenever possible. Only introduce external dependencies when the standard library cannot meet the requirement.

2. **Test-first (TDD)**: Always write unit tests before implementation code. This is a hard rule, not a suggestion.

3. **CLI-first**: All features must be accessible via CLI. The code library (`pkg/`) is a thin wrapper around the service layer.

## Architecture Index

- **Spec (真理之源)**: `docs/superpowers/specs/` — 讨论清楚 specs 之前，禁止做任何事情；主 spec: `2026-04-19-daemon-config-ollama-design.md` (daemon + config + ollama + fusion + memory)
- **Architecture**: Daemon + CLI client-server (HTTP JSON on localhost), CLI auto-starts daemon, `lmd daemon [--detach]`
- **Config**: `~/.config/lmd/config.yaml`, auto-generated on first run
- **Phase 1-4 Plans**: `docs/superpowers/plans/` — 实现计划（当前为空）
- **AGENTS.md**: 项目根目录 — 宪法条令，最高优先级
- **todo.md**: `docs/02.todo.md` — 待定任务，发现变动时 brainstorm 讨论后清空

## Project Structure

```
cmd/lmd/             - CLI entry point
internal/cli/        - Cobra command definitions
internal/dao/        - Database access layer (global dao.DB)
internal/service/    - Business logic layer
internal/tokenizer/  - Text segmentation (gse)
internal/embedding/  - Vector embedding (Ollama HTTP API, sole provider)
internal/chunker/    - Markdown-aware chunking (sliding window, 800 token target)
internal/formatter/  - Output formatting (text/json/md/csv)
internal/daemon/     - Daemon HTTP server + background indexer/embedder
internal/config/     - Config loading (YAML, ~/.config/lmd/config.yaml)
internal/mcp/        - MCP protocol handler (served by daemon)
pkg/                 - Public API for external Go projects
```

## Key Technical Decisions

- **Tokenizer**: go-ego/gse — `seg.SkipLog = true` suppress log noise
- **FTS**: gse pre-tokenize + FTS5 (unicode61 + porter)
- **Vector storage**: sqlite-vec (cosine, 1024 dim)
- **Embedding model**: `qwen3-embedding:0.6b-q8_0` via Ollama
- **Embedding architecture**: Ollama HTTP API (sole provider, no CGo). sqlite-vec uses CGo.
- **Embedding batch**: 8 chunks/batch, rune 级 800 字符截断; Ollama keep_alive=30m, warmup on init
- **Query embedding**: NO Instruct prefix — documents and queries embedded as-is
- **Search granularity**: Chunk-level (FTS on chunks_fts, vector on chunks_vec, results are individual chunks)
- **Score normalization**: BM25: abs/(1+abs), Vector: 1-distance
- **Fusion**: RRF (k=60, 2x 权重 for primary lists, top-rank bonus)，按 ChunkId 合并分数，保留同一文档的多个 chunk（喂给 LLM）
- **HyDE**: HyDE query expansion via Ollama general model (Phase 2)
- **Memory layer**: Agent memories: independent table, fact/episode/relation, exponential time-decay scoring
- **DB indexes**: `documents(collection, path)`, `chunks(doc_id)` — see `internal/dao/schema.go`
- **Timezone**: GMT+8 (CST)
- **SQLite**: WAL mode, no migration system, `CreateTables()` in `schema.go`

## Build & Test

```bash
make build                    # Build CLI (tags: fts5)
make test                     # Run all tests (tags: fts5)
make vet                      # Static analysis
make lint                     # vet + fmt
make e2e                      # End-to-end smoke test (needs Ollama)
make integration-basic        # CLI integration tests
./lmd rebuild                 # Full rebuild (keeps collections)
```

## Reference Projects

- **QMD**: `/Users/xmli/me/code/others/qmd` — TypeScript hybrid search, key reference for HyDE, embedding, reranking
- **Hermes-Agent**: `/Users/xmli/me/code/others/hermes-agent` — Python agent framework

## Pending Tasks

- **HyDE**: todo.md 中有 HyDE (Hypothetical Document Embedding) 查询扩展的待定任务，参考 QMD 实现
