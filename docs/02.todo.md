
This file records temporary development thoughts. When processed, items are resolved and removed. When empty (except this header), all items have been addressed.

## TODO

- [ ] HyDE (Hypothetical Document Embedding) 查询扩展：参考 QMD 的实现，用本地 LLM 生成 lex/vec/hyde 三类查询文本，融合进现有搜索流程。可选方案：A) 直接用 llama-server + 通用模型生成假想文档（低复杂度）；B) 复刻 QMD 微调模型 + GBNF 语法约束（高复杂度）。优先考虑方案 A。
